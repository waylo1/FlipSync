{
  "name": "@flipsync/core",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate:enums": "node scripts/generate-enums.mjs",
    "build": "node scripts/generate-enums.mjs && tsc -b",
    "typecheck": "node scripts/generate-enums.mjs && tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
